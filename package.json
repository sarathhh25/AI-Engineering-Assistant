{
  "name": "ai-engineering-assistant-workspace",
  "private": true,
  "scripts": {
    "dev": "npx -y concurrently \"npm --prefix Frontend run dev\" \"python -m uvicorn Backend.main:app --reload --port 8000\"",
    "dev:frontend": "npm --prefix Frontend run dev",
    "dev:backend": "python -m uvicorn Backend.main:app --reload --port 8000",
    "build": "npm --prefix Frontend run build",
    "start": "npm --prefix Frontend run start",
    "test:e2e": "npm --prefix Frontend run test:e2e",
    "lint": "npm --prefix Frontend run lint"
  }
}
